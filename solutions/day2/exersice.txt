### Exercises

#### Exercises: What is React?

1. What is React?
===> React is Javascript library to buid user interface (UI)
2. What is a library?
====> Libraries in programming languages are collections of prewritten code that users can use to optimize a task
3. What is a single page application?
====> In React js only have 1 HTML file is index.html so that why we call React use to buid single page.
4. What is a component ?
====> Component is a reusable JSX for building web app.
5. What is the latest version of React?
====> 16.13.1
6. What is DOM?
====> DOM is Document Object Model that we can handle and do anything to change HTMl by JS on DOM.
7. What is React Virtual DOM?
====> React Virtual DOM is a own Dom of React and if we change in Virtual DOM it will update exactly change in DOM and do not refresh all DOM
8. What does a web application or a website(composed of) have?
====>

#### Exercises: Why React?

1. Why did you chose to use react?
Fast
Modular
Big company and comunity support
Open source.
Hight offer job.
2. What measures do you use to know popularity ?
Star on Github, Fork on Github, Number user use it.
3. What is more popular, React or Vue ?
Now is React but in future maybe Vue.

#### Exercises: JSX

1. What is an HTML element? 
2. How to write a self closing HTML element?
3. What is an HTML attribute? Write some of them
4. What is JSX?
5. What is babel?
6. What is a transpiler?

#### Exercises: JSX Elements

1. What is a JSX element?
JSX element is a combinator of HTML and JS syntax make we can write HTMl in JS code
2. Write your name in a JSX element and store it in a name variable
const myName = (<p>Le Minh Tri</p>)
3. Write a JSX element which displays your full name, country, title, gender, email, phone number. Use h1 for the name and p for the rest of the information and store it in a user variable
const user =(
    <h1>Le Minh Tri</h1>
    
)
4. Write a footer JSX element

#### Exercises: Inline Style

1. Create a style object for the main JSX
2. Create a style object for the footer and app JSX
3. Add more styles to the JSX elements

#### Exercises: Internal Styles

1. Apply different styles to your JSX elements

#### Exercise: Inject data to JSX

1. Practice how to make JSX element and injecting dynamic data(string, number, boolean, array, object)